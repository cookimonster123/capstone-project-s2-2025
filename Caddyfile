capstones.click {
    redir https://www.capstones.click{uri}
}

www.capstones.click {
    encode gzip

    @api path /api*
    handle @api {
        reverse_proxy server:3000
    }

    handle {
        reverse_proxy client:5173
    }
}
